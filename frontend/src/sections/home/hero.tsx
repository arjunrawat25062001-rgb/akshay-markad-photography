"use client";
import Image from "next/image";
import Link from "next/link";
import { AnimatePresence, motion, useReducedMotion } from "framer-motion";
import { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { Icon } from "@/components/shared/icon";
import { heroReveal } from "@/animations/variants";
import { imageSet } from "@/sections/home/data";
export function Hero() {
  const [active, setActive] = useState(0); const reduce = useReducedMotion();
  useEffect(() => { if (reduce) return; const id = window.setInterval(() => setActive((index) => (index + 1) % imageSet.length), 6000); return () => window.clearInterval(id); }, [reduce]);
  const move = (direction: -1 | 1) => setActive((index) => (index + direction + imageSet.length) % imageSet.length);
  return <section className="relative flex min-h-screen items-end overflow-hidden pb-20 pt-28 sm:items-center sm:pb-0" aria-label="Introduction"><div className="absolute inset-0">{imageSet.map((src, index) => <AnimatePresence key={src}>{index === active && <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} transition={{ duration: 1.1 }} className="absolute inset-0"><motion.div className="absolute inset-0" initial={{ scale: 1 }} animate={{ scale: reduce ? 1 : 1.08 }} transition={{ duration: 6.5, ease: "linear" }}><Image src={src} alt="Akshay Markad editorial photography" fill priority={index === 0} sizes="100vw" className="object-cover" /></motion.div></motion.div>}</AnimatePresence>)}</div><div className="absolute inset-0 bg-gradient-to-r from-black/85 via-black/55 to-black/20" /><div className="absolute inset-x-0 bottom-0 h-2/5 bg-gradient-to-t from-canvas via-canvas/30 to-transparent" /><div className="container-luxury relative z-10"><motion.div initial="hidden" animate="visible" variants={{ visible: { transition: { staggerChildren: 0.16 } } }} className="max-w-3xl"><motion.p variants={heroReveal} className="eyebrow mb-6">Pune · India · Worldwide</motion.p><motion.h1 variants={heroReveal} className="text-display-xl text-white">Capturing Emotions.<br /><em className="font-normal text-gold">Crafting Timeless Memories.</em></motion.h1><motion.p variants={heroReveal} className="mt-6 max-w-2xl text-base leading-7 text-white/75 sm:text-lg">Luxury Wedding, Couple, Pre-Wedding, Event and Maternity Photography based in Pune. Available Across India.</motion.p><motion.div variants={heroReveal} className="mt-9 flex flex-wrap gap-3"><Link href="/contact"><Button>Book a Session</Button></Link><Link href="/portfolio"><Button variant="secondary">View Portfolio</Button></Link><a href="https://wa.me/919000000000" target="_blank" rel="noreferrer"><Button variant="ghost">WhatsApp <Icon name="arrow" className="h-4 w-4" /></Button></a></motion.div></motion.div></div><div className="absolute bottom-7 left-5 z-10 flex items-center gap-3 lg:left-8">{imageSet.map((_, index) => <button key={index} type="button" onClick={() => setActive(index)} className="group h-7 w-7 p-2 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold" aria-label={`Go to slide ${index + 1}`} aria-current={active === index}><span className={`block h-px transition-all ${active === index ? "w-5 bg-gold" : "w-2 bg-white/60 group-hover:w-4"}`} /></button>)}</div><div className="absolute bottom-7 right-5 z-10 flex gap-2 lg:right-8"><button type="button" onClick={() => move(-1)} className="grid h-11 w-11 place-items-center border border-white/20 text-white transition-colors hover:border-gold hover:text-gold focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold" aria-label="Previous slide"><Icon name="arrow" className="h-5 w-5 rotate-180" /></button><button type="button" onClick={() => move(1)} className="grid h-11 w-11 place-items-center border border-white/20 text-white transition-colors hover:border-gold hover:text-gold focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold" aria-label="Next slide"><Icon name="arrow" className="h-5 w-5" /></button></div><motion.div className="absolute bottom-7 left-1/2 z-10 hidden -translate-x-1/2 flex-col items-center gap-2 text-white/60 sm:flex" animate={reduce ? undefined : { y: [0, 7, 0] }} transition={{ duration: 1.6, repeat: Infinity }}><span className="text-[10px] uppercase tracking-[0.25em]">Scroll</span><span className="h-7 w-px bg-gold" /></motion.div></section>;
}
