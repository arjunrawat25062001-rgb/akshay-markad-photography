import { HomePage } from "@/sections/home/home-page";
import { createMetadata } from "@/lib/metadata";
export const metadata = createMetadata({ description: "Luxury wedding, couple, pre-wedding, event and maternity photography by Akshay Markad in Pune and across India." });
export default function Page() { return <HomePage />; }
